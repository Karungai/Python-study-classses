# Constants: fixed values such as numbers, letters and strings
# string constants are when using single quotes (' ') or double quotes (" ")
# variables: allocating a piece od memory and giving it a name. Through the assignment statement. x = 12.2
# naming rules:
# -> use an underscore _name
# -> Case sensitive

# Operator rule precedence
x = 1 + 2 ** 3 / 4 * 5
eee = 'hello' + 'there'
